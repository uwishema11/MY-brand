version: 2.1

jobs:
  test:
   
    docker:
      - image: cimg/node:16.14.0

    steps:
      - checkout   
      - run:
          name: install packages
          command: npm install
      - run:
          name: Run Tests
          command: npm run coverage

workflows:
  orb-free-workflow:
    jobs:
      - test
